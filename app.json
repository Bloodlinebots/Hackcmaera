{
  "name": "CamHackBot",
  "description": "A Telegram bot with WebView & Cloudflare style phishing pages (for educational use only).",
  "repository": "https://github.com/your-username/camhackbot-heroku",
  "keywords": ["telegram", "bot", "webview", "heroku", "ejs", "express"],
  "env": {
    "BOT_TOKEN": {
      "description": "Enter your Telegram bot token here",
      "required": true
    },
    "URL": {
      "description": "Your deployed Heroku app URL (optional but recommended)",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "scripts": {
    "postdeploy": "echo '✅ CamHackBot deployed successfully!'"
  }
}
